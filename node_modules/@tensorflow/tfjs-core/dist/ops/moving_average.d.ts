import { Scalar, Tensor } from '../tensor';
import { TensorLike } from '../types';
declare class MovingAverageOps {
    static movingAverage<T extends Tensor>(v: T | TensorLike, x: T | TensorLike, decay: number | Scalar, step?: number | Scalar, zeroDebias?: boolean): T;
}
export declare const movingAverage: typeof MovingAverageOps.movingAverage;
export {};
