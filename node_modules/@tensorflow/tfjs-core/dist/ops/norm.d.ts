import { Tensor } from '../tensor';
import { TensorLike } from '../types';
declare class NormOps {
    static norm(x: Tensor | TensorLike, ord?: number | 'euclidean' | 'fro', axis?: number | number[], keepDims?: boolean): Tensor;
}
export declare const norm: typeof NormOps.norm;
export {};
