import { Tensor } from '../tensor';
import { TensorLike } from '../types';
declare class StridedSliceOps {
    static stridedSlice<T extends Tensor>(x: T | TensorLike, begin: number[], end: number[], strides: number[], beginMask?: number, endMask?: number): T;
}
export declare const stridedSlice: typeof StridedSliceOps.stridedSlice;
export {};
