import { Tensor3D, Tensor4D } from '../tensor';
import { TensorLike } from '../types';
declare class PoolOps {
    static maxPool<T extends Tensor3D | Tensor4D>(x: T | TensorLike, filterSize: [number, number] | number, strides: [number, number] | number, pad: 'valid' | 'same' | number, dimRoundingMode?: 'floor' | 'round' | 'ceil'): T;
    static avgPool<T extends Tensor3D | Tensor4D>(x: T | TensorLike, filterSize: [number, number] | number, strides: [number, number] | number, pad: 'valid' | 'same' | number, dimRoundingMode?: 'floor' | 'round' | 'ceil'): T;
}
export declare const maxPool: typeof PoolOps.maxPool;
export declare const avgPool: typeof PoolOps.avgPool;
export {};
